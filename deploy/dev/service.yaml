apiVersion: v1
kind: Service
metadata:
  name: cloudkit-console
  namespace: innabox-devel
  labels:
    app: cloudkit-console
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cloudkit-console
